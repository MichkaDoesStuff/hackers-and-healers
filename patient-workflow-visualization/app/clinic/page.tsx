import { ClinicOS } from "@/components/clinic-os"

// Standalone clinic dashboard (local-HAPI demo mode). Preserved here; the app's
// front door is the CDS Hooks Sandbox embed at /sandbox.
export default function ClinicPage() {
  return <ClinicOS />
}
